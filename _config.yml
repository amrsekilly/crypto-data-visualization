# Site settings
title: Blockstream - Crypto Feed
siteurl: http://127.0.0.1:4000
description: "Cryptocurrency data feed"
# edit this to fix the deployment issue (relative path)
baseurl: /test-blockstream

# Build settings
# Sass
sass:
    style: compressed
markdown: kramdown
permalink: pretty

plugins: [jekyll-paginate] 
paginate: 3
paginate_path: "/blog/page:num/"

collections:
    partners:
        output: true
